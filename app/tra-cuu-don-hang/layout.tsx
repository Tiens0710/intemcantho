import { createNoIndexMetadata } from "@/lib/seo";

export const metadata = createNoIndexMetadata("Tra cuu don hang");

export default function OrderLookupLayout({ children }: { children: React.ReactNode }) {
  return children;
}

